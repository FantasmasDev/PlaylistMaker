<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Main.Screen.Theme"
        tools:targetApi="31">
        <activity
            android:name=".ui.player_activity.PlayerActivity"
            android:exported="false"
            android:theme="@style/Second.Screen.Theme"/>
        <activity
            android:name=".ui.search_activity.SearchActivity"
            android:exported="false"
            android:theme="@style/Search.Screen.Theme" />
        <activity
            android:name=".ui.library_activity.LibraryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.settings_activity.SettingsAcivity"
            android:exported="false"
            android:theme="@style/Second.Screen.Theme" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>